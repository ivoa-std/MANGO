The timestamp value is always given as a string. 
The way to process this string is given by the representation mode.
It can represent a date, which must be parsed, or a real value, which must be cast.