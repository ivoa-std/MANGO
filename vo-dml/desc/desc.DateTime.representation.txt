Define the representation mode.
The supported values are \texttt{iso} or \texttt{dateformat} for ISO dates, \texttt{jd} or \texttt{mjd}.
The list of supported values could be extended in the future with new endorsed \texttt{xtype} values.
